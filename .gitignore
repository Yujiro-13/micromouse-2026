# ===== ESP-IDF build artifacts =====
build
managed_components/

# ===== Local editor / tooling =====
.vscode/settings.json
.vscode/c_cpp_properties.json
.cache
.claude
.serena

# ===== Project local files =====
/Report
CLAUDE.md
.mcp.json

# ===== MATLAB generated outputs (tools/matlab) =====
/tools/matlab/emergency_save*
/tools/matlab/temp_save*
/tools/matlab/fre*
/tools/matlab/rotation*

# NOTE: sdkconfig は規約上 .gitignore 対象だが、現環境では idf.py による
#       再生成差分検証ができないため、当面はトラッキングを維持する。
#       ESP-IDF 環境で `idf.py build` し sdkconfig が再生成されることを
#       確認後、`git rm --cached sdkconfig` と下記コメント解除を行うこと。
# sdkconfig
